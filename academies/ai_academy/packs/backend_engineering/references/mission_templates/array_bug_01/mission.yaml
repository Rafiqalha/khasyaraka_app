id: "mission_array_bug_01"
title: "Fix the Off-by-One Error"
difficulty: "beginner"
concept_ids:
  - "concept_array"
  - "concept_iteration"
workspace: "workspace_python"
goal: "The function sum_list() returns the wrong total. Find and fix the bug so all tests pass."
fixture:
  source: "bug_001.py"
  tests: "tests/bug_001_test.py"
  runtime: "python"
evaluation:
  type: "test_runner"
  pass_threshold: 1.0
hints:
  - level: 1
    text: "Look carefully at where the loop starts. Does it visit every element?"
  - level: 2
    text: "What is the first value generated by `range(1, len(numbers))`? What index does the first element have?"
  - level: 3
    text: "Change `range(1, len(numbers))` to `range(0, len(numbers))` — or simply `range(len(numbers))`."
