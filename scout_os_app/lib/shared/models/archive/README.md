# Archived Models

These domain models were moved here from `core/domain/models/` during the Repository Cleanup sprint.

They are **not currently used** in any active feature module, but may be reused when the following Learning OS components are built:

- `Evidence` → Evidence Validator / Portfolio Generator
- `Assessment` → Pre-Assessment Engine
- `Operation` → Cybersecurity Operations v2
- `Patrol` → Field Operations / Location-based activities
- `Incident` → Incident reporting (if revived)

**Do not delete permanently** until the roadmap for these features is finalized.
