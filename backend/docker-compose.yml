version: '3.8'

services:
  db:
    image: postgres:15-alpine
    container_name: pradigi-db
    restart: unless-stopped
    environment:
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: SangatRahasia123!
      POSTGRES_DB: pradigi
    ports:
      - "5432:5432"
    volumes:
      - pgdata:/var/lib/postgresql/data

  redis:
    image: redis:7-alpine
    container_name: pradigi-redis
    restart: unless-stopped
    ports:
      - "6379:6379"

  backend:
    build: .
    container_name: pradigi-server
    restart: unless-stopped
    depends_on:
      - db
      - redis
    env_file:
      - .env
    environment:
      - DATABASE_URL=postgresql://postgres:SangatRahasia123!@db:5432/pradigi?sslmode=disable
      - REDIS_URL=redis://redis:6379/0
      - JWT_SECRET=pradigi-super-secret-jwt-key-production-2026!
      - PORT=8080
      - DEEPSEEK_API_KEY=${DEEPSEEK_API_KEY}
      - GEMINI_API_KEY=${GEMINI_API_KEY}
      - APP_ENV=production
    ports:
      - "8080:8080"

volumes:
  pgdata:
