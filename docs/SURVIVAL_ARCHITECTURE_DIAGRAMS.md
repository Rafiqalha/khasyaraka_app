# 🏗️ Survival Module - Architecture Diagrams

## System Architecture: Before vs After

### BEFORE: Gamified Model (with Backend Dependency)
```
┌─────────────────────────────────────────────────────────────────┐
│                    Flutter App (scout_os_app)                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Survival Module (Game-like Progression)                  │  │
│  │                                                            │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ SurvivalMasteryPage                                │  │  │
│  │  │ • Show levels 1-6                                  │  │  │
│  │  │ • Show XP progress bars                            │  │  │
│  │  │ • Locked/Unlocked tools                            │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │                       ▼                                    │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ SurvivalMasteryController                          │  │  │
│  │  │ • Tracks XP, Levels, Progress                      │  │  │
│  │  │ • Makes API calls for every action                 │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │                       ▼ (Dio HTTP)                        │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
└───────────────┬──────────────────────────────────────────────────┘
                │
         (HTTP Requests)
                │
       ┌────────▼──────────┐
       │ FastAPI Backend   │
       │ /survival/mastery │
       │ /survival/action  │
       └────────┬──────────┘
                │
       ┌────────▼──────────┐
       │  PostgreSQL DB    │
       │ survival_mastery  │
       └───────────────────┘

⚠️ PROBLEMS:
  • Requires internet connection
  • Slow (network latency)
  • Complex database schema
  • Unnecessary XP tracking
  • Backend dependency
```

---

### AFTER: Offline Utility Toolkit
```
┌─────────────────────────────────────────────────────────────────┐
│                    Flutter App (scout_os_app)                   │
├─────────────────────────────────────────────────────────────────┤
│                                                                   │
│  ┌──────────────────────────────────────────────────────────┐  │
│  │ Survival Module (Pure Utility Toolkit)                   │  │
│  │                                                            │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ SurvivalDashboardPage                              │  │  │
│  │  │ ┌──────────────┬──────────────┬──────────────────┐ │  │  │
│  │  │ │ 🧭 Compass   │ 📐 Clinometer│ 📍 GPS Tracker   │ │  │  │
│  │  │ │ [Tap to use] │ [Tap to use] │ [Tap to use]    │ │  │  │
│  │  │ └──────────────┴──────────────┴──────────────────┘ │  │  │
│  │  │ • No levels                                         │  │  │
│  │  │ • No XP                                             │  │  │
│  │  │ • All tools instantly available                     │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │                       ▼                                    │  │
│  │  ┌────────────────────────────────────────────────────┐  │  │
│  │  │ SurvivalToolsController                            │  │  │
│  │  │ • Manages local sensor streams only                │  │  │
│  │  │ • NO API calls                                     │  │  │
│  │  │ • Compass, Accelerometer, GPS direct access       │  │  │
│  │  └────────────────────────────────────────────────────┘  │  │
│  │        ▼           ▼           ▼                          │  │
│  │  ┌─────────────────────────────────────┐                 │  │
│  │  │   Device Sensors (100% Offline)     │                 │  │
│  │  │ • Magnetometer (Compass)            │                 │  │
│  │  │ • Accelerometer (Clinometer)        │                 │  │
│  │  │ • GPS Receiver (Location)           │                 │  │
│  │  └─────────────────────────────────────┘                 │  │
│  │                                                            │  │
│  └──────────────────────────────────────────────────────────┘  │
│                                                                   │
│  (Optional: Light backend for tool config)                       │
└─────────────────────────────────────────────────────────────────┘

✅ BENEFITS:
  • 100% offline - works without internet
  • Ultra-fast (no network latency)
  • Minimal dependencies
  • Direct device sensor access
  • Battery optimized
  • User privacy (no location tracking)
```

---

## Data Flow Diagrams

### Compass Tool Flow
```
User Opens App
    │
    ▼
SurvivalDashboardPage initializes
    │
    ▼
Call: controller.initializeSensors()
    │
    ▼
SurvivalToolsController._initializeCompass()
    │
    ▼
compassEvents.listen() ◄──── Device Magnetometer
    │                         (hardware stream)
    ▼
CompassData {heading: 45.2°, magneticField: 50μT}
    │
    ▼
notifyListeners() ◄──── Provider refresh
    │
    ▼
CompassToolPage._CompassPreview updates UI
    │
    ▼
User sees: "45.2° NE" with animated needle

⏱️ Latency: < 50ms (direct hardware)
🔌 Internet: NOT REQUIRED
```

---

### Clinometer (Accelerometer) Tool Flow
```
User taps Clinometer card
    │
    ▼
ClinometerToolPage opens
    │
    ▼
Consumer<SurvivalToolsController> watches clinoData
    │
    ▼
SurvivalToolsController._initializeClinometer()
    │
    ▼
accelerometerEvents.listen() ◄──── Device Accelerometer
    │                               (hardware stream)
    ▼
Raw acceleration: (x, y, z) in m/s²
    │
    ▼
Calculate angles using atan2:
    • Pitch = atan2(y, z) → Forward/back tilt
    • Roll = atan2(x, z) → Left/right tilt
    • Yaw = atan2(x, y) → Rotation
    │
    ▼
ClinoData {pitch: 12.5°, roll: 3.2°, yaw: 45.1°}
    │
    ▼
notifyListeners() ◄──── Provider refresh
    │
    ▼
ClinometerToolPage updates 3 angle cards
    │
    ▼
User sees: Real-time 3D orientation

⏱️ Latency: < 30ms (direct hardware)
🔌 Internet: NOT REQUIRED
🔋 Battery: Minimal (direct hardware access)
```

---

### GPS Tool Flow
```
User taps GPS Tracker card
    │
    ▼
GpsTrackerToolPage opens
    │
    ▼
SurvivalToolsController._initializeGps()
    │
    ▼
Check location permission
    ├─ Granted? → Continue
    └─ Denied? → Show error + Ask permission
    │
    ▼
Geolocator.getCurrentPosition()
    │
    ▼
Device GPS receiver acquires signal
    │
    └─ Takes 5-30 seconds first time
    └─ Uses cached ephemeris if available
    │
    ▼
Position {lat, lng, altitude, accuracy, speed}
    │
    ▼
Geolocator.getPositionStream() ◄──── Continuous updates
    │                                 (Every 5m or configurable)
    ▼
GpsData {latitude, longitude, altitude, accuracy, speed}
    │
    ▼
notifyListeners() ◄──── Provider refresh
    │
    ▼
GpsTrackerToolPage updates coordinates
    │
    ▼
User sees: 37.7749°N, 122.4194°W, 45m altitude

⏱️ Latency: First lock 5-30s, then <2s updates
🔌 Internet: NOT REQUIRED (GPS is independent)
🔋 Battery: Moderate (GPS is power-hungry)
```

---

## State Management Architecture

### OLD: ChangeNotifier with API
```
┌─────────────────────────────────────────┐
│ SurvivalMasteryController               │
│ (extends ChangeNotifier)                │
├─────────────────────────────────────────┤
│                                         │
│ State:                                  │
│  • _masteryData: AllMasteryResponse    │ ◄─ From API
│  • _isLoading: bool                    │
│  • _errorMessage: String?              │
│                                         │
│ Methods:                                │
│  • loadMastery() → API call            │
│  • recordAction() → API call           │
│  • notifyListeners()                   │
│                                         │
└─────────────────────────────────────────┘
        │
        │ depends on
        ▼
┌─────────────────────────────────────────┐
│ SurvivalRepository                      │
├─────────────────────────────────────────┤
│  • fetchMastery() → HTTP Dio call      │
│  • recordAction() → HTTP Dio call      │
│                                         │
└─────────────────────────────────────────┘
        │
        │ HTTP
        ▼
    Backend API
```

---

### NEW: Direct Sensor Streams
```
┌──────────────────────────────────────────────────────┐
│ SurvivalToolsController                              │
│ (extends ChangeNotifier)                             │
├──────────────────────────────────────────────────────┤
│                                                      │
│ State:                                               │
│  • _compassData: CompassData?      ◄─ From Sensor  │
│  • _clinoData: ClinoData?          ◄─ From Sensor  │
│  • _gpsData: GpsData?              ◄─ From Sensor  │
│  • _compassError: String?                           │
│  • _clinoError: String?                            │
│  • _gpsError: String?                              │
│                                                      │
│ Methods:                                             │
│  • initializeSensors()                              │
│  • _initializeCompass()    ◄─ Local sensor          │
│  • _initializeClinometer() ◄─ Local sensor          │
│  • _initializeGps()        ◄─ Local sensor          │
│  • notifyListeners()       ◄─ On sensor update      │
│                                                      │
└──────────────────────────────────────────────────────┘
        │           │           │
        │ listens   │ listens   │ listens
        ▼           ▼           ▼
    ┌──────────┬──────────┬──────────────┐
    │Magnetom. │Accelero. │GPS Receiver  │
    │(Direct)  │(Direct)  │(Direct)      │
    │<30ms lag │<30ms lag │<2000ms lag   │
    └──────────┴──────────┴──────────────┘
    (100% Offline - No Network Required)
```

---

## Tool-Specific Architectures

### Compass Component
```
┌─────────────────────────────────────────┐
│ CompassToolPage                         │
├─────────────────────────────────────────┤
│                                         │
│ Consumer<SurvivalToolsController>       │
│   watches: controller.compassData       │
│                                         │
│  ┌────────────────────────────────────┐ │
│  │ CircularProgressIndicator loading  │ │
│  └────────────────────────────────────┘ │
│           (until data arrives)          │
│                                         │
│  ┌────────────────────────────────────┐ │
│  │ Compass Rose                       │ │
│  │ • Circle with N/S/E/W labels       │ │
│  │ • Green border                     │ │
│  │ • Animated needle                  │ │
│  │   Transform.rotate(angle)          │ │
│  └────────────────────────────────────┘ │
│                                         │
│  ┌────────────────────────────────────┐ │
│  │ Display: "45.2° NE"                │ │
│  │ • Degree value from heading        │ │
│  │ • Cardinal direction computed      │ │
│  └────────────────────────────────────┘ │
│                                         │
│  ┌────────────────────────────────────┐ │
│  │ Info Card                          │ │
│  │ • Magnetic Field: 50 μT            │ │
│  │ • Accuracy: 2.5°                   │ │
│  └────────────────────────────────────┘ │
│                                         │
└─────────────────────────────────────────┘
```

---

### GPS Component
```
┌─────────────────────────────────────────┐
│ GpsTrackerToolPage                      │
├─────────────────────────────────────────┤
│                                         │
│ Consumer<SurvivalToolsController>       │
│   watches: controller.gpsData           │
│                                         │
│  ┌────────────────────────────────────┐ │
│  │ Status Banner                      │ │
│  │ "✓ GPS LOCKED - Accuracy: Good"   │ │
│  └────────────────────────────────────┘ │
│                                         │
│  ┌────────────────────────────────────┐ │
│  │ Latitude Card                      │ │
│  │ "37.7749°N"                        │ │
│  └────────────────────────────────────┘ │
│                                         │
│  ┌────────────────────────────────────┐ │
│  │ Longitude Card                     │ │
│  │ "122.4194°E"                       │ │
│  └────────────────────────────────────┘ │
│                                         │
│  ┌────────────────────────────────────┐ │
│  │ Altitude & Speed Row               │ │
│  │ ┌──────────────┬──────────────────┐│ │
│  │ │ 45.2m        │ 2.5 km/h        ││ │
│  │ │ Low altitude │ Moving          ││ │
│  │ └──────────────┴──────────────────┘│ │
│  └────────────────────────────────────┘ │
│                                         │
│  ┌────────────────────────────────────┐ │
│  │ Accuracy Details                   │ │
│  │ ┌──────────────┐ "5.2m"            │ │
│  │ │████████░    │ [Progress bar]    │ │
│  │ │ Excellent   │ (< 5m = green)    │ │
│  │ └──────────────┘                  │ │
│  └────────────────────────────────────┘ │
│                                         │
│  ┌────────────────────────────────────┐ │
│  │ Decimal Coordinates (Tap to copy)  │ │
│  │ 37.774900                          │ │
│  │ 122.419400                         │ │
│  └────────────────────────────────────┘ │
│                                         │
└─────────────────────────────────────────┘
```

---

## Sensor Update Rate Comparison

```
Tool          │ Sensor        │ Update Rate    │ Latency │ Battery Impact
──────────────┼───────────────┼────────────────┼─────────┼───────────────
Compass       │ Magnetometer  │ 30-100 Hz      │ <50ms   │ Very Low
Clinometer    │ Accelerometer │ 50-200 Hz      │ <30ms   │ Very Low
GPS           │ GPS Receiver  │ 1 Hz (1 fix/s) │ <2000ms │ High

Magnetometer: Most efficient, runs continuously without battery drain
Accelerometer: Efficient, always-on sensor
GPS: Power-intensive, use sparingly, but necessary for location
```

---

## Error Handling Flow

```
User Opens Tool
    │
    ▼
Try: Initialize Sensor
    │
    ├─ Success ──▶ Setup listener stream ──▶ Display data
    │
    └─ Fail ──▶ Catch exception
                │
                ▼
            Set errorMessage
                │
                ▼
            notifyListeners()
                │
                ▼
            Display Error Widget
            "⚠️ Sensor Not Available"
            (with retry button)
```

---

## Offline Guarantee

```
┌─────────────────────────────────────────────────────────┐
│ All 3 Survival Tools are 100% OFFLINE                  │
├─────────────────────────────────────────────────────────┤
│                                                         │
│ ✓ Compass                                             │
│   - Uses built-in magnetometer                        │
│   - No internet needed                                │
│   - No API calls                                      │
│   - Works even if WiFi/cellular disabled             │
│                                                       │
│ ✓ Clinometer                                          │
│   - Uses built-in accelerometer                       │
│   - No internet needed                                │
│   - No API calls                                      │
│   - Works offline always                              │
│                                                       │
│ ✓ GPS Tracker                                         │
│   - Uses built-in GPS receiver                        │
│   - No internet needed for location                   │
│   - No API calls                                      │
│   - Works offline in airplane mode                    │
│   - (Optional assisted GPS possible but not required) │
│                                                       │
│ Backend is OPTIONAL:                                  │
│   - If /tools/config endpoint is down: App still works│
│   - If backend is offline: All tools work fine        │
│   - Backend only serves static config, not required   │
│                                                       │
└─────────────────────────────────────────────────────────┘
```

---

**Architecture Documentation**  
**Generated:** February 5, 2026  
**Status:** Complete
